pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh "cd project1-python-flask"
                sh "ls"
                sh "docker image ls"
            }
            
        }
        
    }
}
